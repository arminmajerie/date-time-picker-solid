{
  "name": "date-time-picker-solid",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w dtp-example",
    "build": "npm run build -w @arminmajerie/dtp-solid",
    "build:all": "npm run build -w @arminmajerie/dtp-solid && npm run build -w dtp-example",
    "preview": "npm run preview -w dtp-example"
  }
}
